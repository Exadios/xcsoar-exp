audio = subproject('audio_sources')
audio_sources = audio.get_variable('audio_sources')

audio_lib = library('audio', audio_sources,
                   include_directories : common_includes
                  )
