waypoint = subproject('waypoint_sources')
waypoint_sources = waypoint.get_variable('waypoint_sources')

waypoint_lib = library('waypoint', waypoint_sources,
                   include_directories : common_includes
                  )
