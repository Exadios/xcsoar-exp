route = subproject('route_sources')
route_sources = route.get_variable('route_sources')

route_lib = library('route', route_sources,
                   include_directories : common_includes
                  )
