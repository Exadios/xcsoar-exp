driver = subproject('driver_sources')
driver_sources = driver.get_variable('driver_sources')

driver_lib = library('driver', driver_sources,
                   include_directories : common_includes
                  )
