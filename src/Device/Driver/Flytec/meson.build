flytec = subproject('flytec_sources')
flytec_sources = flytec.get_variable('flytec_sources')

flytec_lib = library('flytec', flytec_sources,
                   include_directories : common_includes
                  )
