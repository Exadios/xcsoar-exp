adsb = subproject('adsb_sources')
adsb_sources = adsb.get_variable('adsb_sources')

adsb_lib = library('adsb', adsb_sources,
                   include_directories : common_includes
                  )
