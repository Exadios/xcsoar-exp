thermalexpress = subproject('thermalexpress_sources')
thermalexpress_sources = thermalexpress.get_variable('thermalexpress_sources')

thermalexpress_lib = library('thermalexpress', thermalexpress_sources,
                   include_directories : common_includes
                  )
