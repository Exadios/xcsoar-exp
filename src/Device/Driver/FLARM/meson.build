flarm = subproject('flarm_sources')
flarm_sources = flarm.get_variable('flarm_sources')

flarm_lib = library('flarm', flarm_sources,
                   include_directories : common_includes
                  )
