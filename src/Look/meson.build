look = subproject('look_sources')
look_sources = look.get_variable('look_sources')

look_lib = library('look', look_sources,
                   include_directories : common_includes
                  )
