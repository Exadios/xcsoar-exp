util = subproject('util_sources')
util_sources = util.get_variable('util_sources')

util_lib = library('util', util_sources,
                   include_directories : common_includes
                  )
