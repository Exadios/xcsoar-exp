http = subproject('http_sources')
http_sources = http.get_variable('http_sources')

http_lib = library('http', http_sources,
                   include_directories : common_includes
                  )
