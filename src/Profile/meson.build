profile = subproject('profile_sources')
profile_sources = profile.get_variable('profile_sources')

profile_lib = library('profile', profile_sources,
                   include_directories : common_includes
                  )
