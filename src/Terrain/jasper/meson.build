jasper = subproject('jasper_sources')
jasper_sources = jasper.get_variable('jasper_sources')

jasper_lib = library('jasper', jasper_sources,
                   include_directories : common_includes
                  )
