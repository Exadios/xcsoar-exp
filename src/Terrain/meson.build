terrain = subproject('terrain_sources')
terrain_sources = terrain.get_variable('terrain_sources')

terrain_lib = library('terrain', terrain_sources,
                   include_directories : common_includes
                  )
