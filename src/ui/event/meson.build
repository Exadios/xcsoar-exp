event = subproject('event_sources')
event_sources = event.get_variable('event_sources')

event_lib = library('event', event_sources,
                   include_directories : common_includes
                  )
