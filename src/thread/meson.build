thread = subproject('thread_sources')
thread_sources = thread.get_variable('thread_sources')

thread_lib = library('thread', thread_sources,
                   include_directories : common_includes
                  )
