form = subproject('form_sources')
form_sources = form.get_variable('form_sources')

form_lib = library('form', form_sources,
                   include_directories : common_includes
                  )
