infobox = subproject('infobox_sources')
infobox_sources = infobox.get_variable('infobox_sources')

infobox_lib = library('infobox', infobox_sources,
                   include_directories : common_includes
                  )
