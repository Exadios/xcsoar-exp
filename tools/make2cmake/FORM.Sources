#!/bin/bash

. CommonVars

pushd ${MAKEBASEDIR} > /dev/null
make -pn TARGET=UNIX | grep ^FORM_SOURCES | sed -e 's:\$(SRC):${XCSOAR_SRC}:g' | sed -e 's:\$(FORM_SRC_DIR):${FORM_DIR}:g' | cut -d '=' -f 2 -
popd > /dev/null

